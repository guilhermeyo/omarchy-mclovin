See [AGENTS.md](AGENTS.md).

It is the same instructions for every agent, kept in one file so they cannot
drift apart — which is the failure mode this repository has spent the most time
fixing everywhere else.
